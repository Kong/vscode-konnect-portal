// Minimal mock file to provide import resolution for 'vscode' in tests
// Each test file handles its own mocking via vi.mock('vscode')

// Export empty object - the real exports come from the per-test mocks
export {}
