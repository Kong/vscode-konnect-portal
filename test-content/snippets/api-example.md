# API Snippet Example

This is a code snippet for our API documentation.

```javascript
const response = await fetch('/api/users')
const users = await response.json()
console.log(users)
```

Use this snippet in your documentation pages.
